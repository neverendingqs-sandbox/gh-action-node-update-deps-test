name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: neverendingqs/gh-action-node-update-deps@10d83bbcde6581aa4c17873195aa747420104e99
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
